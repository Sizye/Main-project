routine main(data : array[] real, count : integer) : real is
    var i : integer;
    for j in 1..size(data) loop
        i := 34;
    end
    i := data[100000000];
    var sum : real is 0.0;
    var avg : real;
    
    
    if count <= 0 then
        return 0.0;
    end
    
    for i in 1..size(data) loop
        if i <= count then
            sum := sum + data[-1];
        end
    end
    
    avg := sum / count;
    return avg;
end
