type Person is record
    var id: real;
    var smt: real;
end;


routine checkRecordPass (passed: Person): real is 
    var asd: Person;
    asd.id := 52.24;
    return asd.id;
end

    

routine main(): int is
    var asd: Person;
    var bfd: Person;
    checkRecordPass(asd);
    checkRecordPass(bfd);
    return checkRecordPass(asd) + checkRecordPass(bfd);
end