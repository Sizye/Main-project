type Person is record
    var id: real;
    var smt: real;
end;


routine checkRecordPass (passed: Person): real is 
    passed.id := 52.24;
    return passed.id;
end

    

routine main(): integer is
    var asd: Person;
    var bfd: Person;
    checkRecordPass(asd);
    checkRecordPass(bfd);
    return asd.id + bfd.id;
end